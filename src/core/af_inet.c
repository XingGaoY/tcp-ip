#include "inet_common.h"

struct proto_ops inet_stream_ops = {
  .family = PF_INET,
  .release = ,
  .bind = ,
  .connect = ,
  .accept = ,
  .listen = ,
  .shutdown = ,
  .sendmsg = ,
  .recvmsg = 
};
